{
  "mcpServers": {
    "playwright": {
      "command": "npx.cmd",
      "args": ["@playwright/mcp@0.0.70", "--headless=false"],
      "env": {
        "PLAYWRIGHT_MCP_LOG_DIR": ".playwright-mcp"
      }
    }
  }
}